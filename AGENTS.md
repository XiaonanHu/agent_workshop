# AGENTS.md

## Cursor Cloud specific instructions

This repository (`agent_workshop`) is a blank scaffold. As of initial setup, it contains only a `README.md`.

### Environment

- **Node.js**: v22 (via nvm)
- **Python**: 3.12
- **Package managers available**: npm, pnpm, yarn, pip

### Notes for future agents

- There are no application services, dependencies, or build systems configured yet.
- When code is added, update this file with instructions for running lint, tests, builds, and dev servers.
- The update script (`echo "No dependencies to install"`) is a no-op placeholder. Replace it once a package manager manifest (e.g. `package.json`, `requirements.txt`) is added.
